runpod==1.7.3
# QwenImageEditPlusPipeline is not in a stable diffusers release yet
git+https://github.com/huggingface/diffusers
# 5.x added continuous_batching which breaks on PyTorch 2.4 base image
# 4.52+ has Qwen2_5_VLForConditionalGeneration; lower bound forces upgrade over base-image version
transformers>=4.52.0,<5.0.0
accelerate>=0.27.0
# Pin torch to match base image version — prevents accidental upgrade
torch==2.6.0
Pillow>=10.0.0
huggingface_hub>=0.23.0
hf_transfer>=0.1.4
