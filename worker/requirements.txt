runpod==1.7.3
# QwenImageEditPlusPipeline is not in a stable diffusers release yet
git+https://github.com/huggingface/diffusers
# 4.49+ added continuous_batching which requires torch internals not in 2.4.0 base
transformers==4.48.0
accelerate>=0.27.0
# Pin torch to match the base image — prevents pip from upgrading it
torch==2.4.0
Pillow>=10.0.0
huggingface_hub>=0.23.0
